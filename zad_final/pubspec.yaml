name: zad_food_rescue
description: ZAD – Reduce Waste, Feed the Community. A food rescue app connecting donors, charities, and volunteers.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.2.1
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
